<div class="container">

  <table class="table table-striped table-bordered">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
    </tr>

    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.First_Name__c %></td>
        <td><%= contact.Last_Name__c %></td>
        <td><%= contact.Email__c %></td>
      </tr>
    <% end %>
  </table>

</div>
